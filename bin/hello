#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "colorize"
require "dotenv/load"

app_name = ENV["APP_NAME"].to_s.strip
app_name = "(missing APP_NAME)" if app_name.empty?
puts "hello from #{app_name}".green
