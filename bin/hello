#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "colorize"
require "dotenv/load"

app_name = ENV.fetch("APP_NAME", "(missing APP_NAME)")
puts "hello from #{app_name}".green
